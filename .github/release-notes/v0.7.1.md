## What's new

* Release notes are now shown directly inside the app after an update.
* Replaced the VLC-based video player with the built-in Windows media player, reducing the app size and improving startup time.

## Fixes

* Encoder validation now uses a generated test video instead of capturing the PullWatch window, making setup more reliable and preventing calibration from getting stuck.
