<Window x:Class="PullWatch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PullWatch"
        Title="PullWatch"
        Width="1180"
        Height="760"
        MinWidth="900"
        MinHeight="600"
        Icon="Assets/favicon.ico"
        WindowStartupLocation="CenterScreen">
    <Grid Background="{StaticResource WindowBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{StaticResource SidebarBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,1,0"
                Padding="20,24">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock FontSize="26"
                               FontWeight="SemiBold"
                               Text="PullWatch" />
                    <TextBlock Margin="0,4,0,20"
                               Style="{StaticResource SecondaryTextStyle}"
                               Text="Gameplay recorder" />

                    <local:RecordingStatusCard Margin="0,0,0,22"
                                               DataContext="{Binding Recordings}" />
                </StackPanel>

                <TextBlock DockPanel.Dock="Bottom"
                           Margin="0,24,0,0"
                           Style="{StaticResource SecondaryTextStyle}"
                           Text="{Binding AppVersionLabel}" />

                <ListBox ItemsSource="{Binding NavigationItems}"
                         SelectedItem="{Binding SelectedNavigationItem}"
                         Style="{StaticResource NavigationListStyle}"
                         ItemContainerStyle="{StaticResource NavigationItemStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Width="26"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="15"
                                           Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}"
                                           Text="{Binding Glyph}" />
                                <TextBlock FontSize="14"
                                           Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}"
                                           Text="{Binding Title}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <ContentControl Grid.Column="1"
                        Content="{Binding SelectedNavigationItem.Content}" />
    </Grid>
</Window>
